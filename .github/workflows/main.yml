name: Gemini Architect and Reviewer

on:
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      request_id:
        description: 'Correlation request ID for Kilo->Gemini orchestration'
        required: true
        type: string
      task:
        description: 'Task description for Gemini execution'
        required: true
        type: string
      repository:
        description: 'Repository name (owner/repo)'
        required: true
        type: string
      base_branch:
        description: 'Base branch for the task'
        required: true
        type: string
      kilo_execution_id:
        description: 'Kilo execution identifier from completed Kilo run'
        required: true
        type: string

jobs:
  advisory:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (
        github.event_name == 'issue_comment' &&
        startsWith(github.event.comment.body, '@gemini-cli') &&
        contains(fromJSON('["OWNER", "MEMBER", "COLLABORATOR"]'), github.event.comment.author_association)
      )
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Extract Gemini request (issue_comment)
        id: request_comment
        if: github.event_name == 'issue_comment'
        env:
          COMMENT_BODY: ${{ github.event.comment.body }}
        shell: bash
        run: |
          set -euo pipefail

          REQUEST="${COMMENT_BODY#@gemini-cli}"
          REQUEST="${REQUEST# }"

          {
            echo 'request<<EOF'
            printf '%s\n' "$REQUEST"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Prepare orchestration context (workflow_dispatch)
        id: orchestration_context
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          set -euo pipefail

          {
            echo 'request_id<<EOF'
            printf '%s\n' "${{ github.event.inputs.request_id }}"
            echo 'EOF'
            echo 'task<<EOF'
            printf '%s\n' "${{ github.event.inputs.task }}"
            echo 'EOF'
            echo 'repository<<EOF'
            printf '%s\n' "${{ github.event.inputs.repository }}"
            echo 'EOF'
            echo 'base_branch<<EOF'
            printf '%s\n' "${{ github.event.inputs.base_branch }}"
            echo 'EOF'
            echo 'kilo_execution_id<<EOF'
            printf '%s\n' "${{ github.event.inputs.kilo_execution_id }}"
            echo 'EOF'
          } >> "$GITHUB_OUTPUT"

      - name: Run Gemini in advisory mode
        id: gemini_run
        uses: google-github-actions/run-gemini-cli@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GEMINI_CLI_TRUST_WORKSPACE: "true"
          ORCHESTRATION_REQUEST_ID: ${{ steps.orchestration_context.outputs.request_id }}
          ORCHESTRATION_TASK: ${{ steps.orchestration_context.outputs.task }}
          ORCHESTRATION_REPOSITORY: ${{ steps.orchestration_context.outputs.repository }}
          ORCHESTRATION_BASE_BRANCH: ${{ steps.orchestration_context.outputs.base_branch }}
          ORCHESTRATION_KILO_EXECUTION_ID: ${{ steps.orchestration_context.outputs.kilo_execution_id }}
        with:
          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
          gemini_model: gemini-3.1-flash-lite
          github_issue_number: ${{ github.event.issue.number }}
          github_pr_number: ${{ github.event.pull_request.number }}
          prompt: |
            You are Gemini, the Fluent with Kyle OpenClaw project's
            Architect, Reviewer, and Research agent.

            MODE: NORMAL ADVISORY MODE

            In this mode you are advisory only.

            You may:
            - inspect the repository;
            - inspect ARCHITECTURE.md and GEMINI.md;
            - analyze architecture and implementation;
            - review code and integrations;
            - research repository state;
            - produce implementation plans;
            - identify affected files, risks, validation steps, and
              acceptance criteria.

            You must NOT:
            - modify repository files;
            - create, modify, rename, move, or delete files;
            - commit or push;
            - create or merge pull requests;
            - otherwise change repository state.

            {{#if env.ORCHESTRATION_REQUEST_ID}}
            ORCHESTRATION CONTEXT (from Kilo->Gemini workflow_dispatch):
            - request_id: ${{ env.ORCHESTRATION_REQUEST_ID }}
            - task: ${{ env.ORCHESTRATION_TASK }}
            - repository: ${{ env.ORCHESTRATION_REPOSITORY }}
            - base_branch: ${{ env.ORCHESTRATION_BASE_BRANCH }}
            - kilo_execution_id: ${{ env.ORCHESTRATION_KILO_EXECUTION_ID }}

            This task was dispatched from the Kilo orchestration layer. The request_id
            correlates this Gemini execution with the originating Kilo completion.
            {{else}}
            The request below was explicitly submitted by a trusted
            repository collaborator through the @gemini-cli interface.

            Treat that request as the actual task.
            {{/if}}

            {{#if env.ORCHESTRATION_REQUEST_ID}}
            --- ORCHESTRATION TASK START ---
            ${{ env.ORCHESTRATION_TASK }}
            --- ORCHESTRATION TASK END ---
            {{else}}
            --- REQUEST START ---
            ${{ steps.request_comment.outputs.request || 'Provide an architecture/status assessment of the current repository.' }}
            --- REQUEST END ---
            {{/if}}

            Read ARCHITECTURE.md and GEMINI.md before responding.
            Distinguish observations from recommendations.
            Return a concrete, actionable response for the project.

      - name: Prepare callback payload (workflow_dispatch only)
        id: callback_payload
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          set -euo pipefail

          REQUEST_ID="${{ steps.orchestration_context.outputs.request_id }}"
          TASK="${{ steps.orchestration_context.outputs.task }}"
          REPOSITORY="${{ steps.orchestration_context.outputs.repository }}"
          BASE_BRANCH="${{ steps.orchestration_context.outputs.base_branch }}"
          KILO_EXECUTION_ID="${{ steps.orchestration_context.outputs.kilo_execution_id }}"

          # Capture Gemini output (stdout from run-gemini-cli)
          GEMINI_OUTPUT="${{ steps.gemini_run.outputs.result }}"

          # Determine status based on Gemini execution
          # For advisory mode, we treat completion as success unless there's an error
          STATUS="success"
          BLOCKERS="[]"

          # Build ACP execution report using jq for JSON-safe serialization
          INVOCATION_ID="gemini-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          RUN_ID="${GITHUB_RUN_ID}"

          jq -n \
            --arg request_id "$REQUEST_ID" \
            --arg agent "Gemini" \
            --arg status "$STATUS" \
            --arg task "$TASK" \
            --arg repository "$REPOSITORY" \
            --arg base_branch "$BASE_BRANCH" \
            --arg gemini_output "$GEMINI_OUTPUT" \
            --arg invocation_id "$INVOCATION_ID" \
            --arg run_id "$RUN_ID" \
            --argjson blockers "$BLOCKERS" \
            '{
              request_id: $request_id,
              agent: $agent,
              status: $status,
              task: $task,
              repository: $repository,
              base_branch: $base_branch,
              changed_files: [],
              verification: ["advisory review completed"],
              result: {
                execution_metadata: {
                  invocation_id: $invocation_id,
                  run_id: $run_id
                },
                gemini_output: $gemini_output
              },
              commit: null,
              push: false,
              blockers: $blockers
            }' > callback_payload.json

          cat callback_payload.json

      - name: Send callback to Render (workflow_dispatch only)
        id: send_callback
        if: github.event_name == 'workflow_dispatch'
        env:
          CALLBACK_URL: ${{ secrets.RENDER_GEMINI_CALLBACK_URL }}
          CALLBACK_SECRET: ${{ secrets.GEMINI_CALLBACK_SECRET }}
        shell: bash
        run: |
          set -euo pipefail

          if [ -z "$CALLBACK_URL" ] || [ -z "$CALLBACK_SECRET" ]; then
            echo "::warning::Callback URL or secret not configured, skipping callback"
            echo "callback_sent=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -H "x-gemini-callback-secret: $CALLBACK_SECRET" \
            -d @callback_payload.json)

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | head -n-1)

          echo "Callback response: $HTTP_CODE"
          echo "Callback body: $BODY"

          if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
            echo "callback_sent=true" >> "$GITHUB_OUTPUT"
          else
            echo "callback_sent=false" >> "$GITHUB_OUTPUT"
            echo "::error::Callback failed with HTTP $HTTP_CODE: $BODY"
            exit 1
          fi